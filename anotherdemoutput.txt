(.venv) (base) thembo@workspace:~/fedora/dev/nlpdemo$ python anotherdemo.py 
/home/thembo/fedora/dev/nlpdemo/.venv/lib/python3.6/site-packages/transformers/configuration_utils.py:337: UserWarning: Passing `gradient_checkpointing` to a config initialization is deprecated and will be removed in v5 Transformers. Using `model.gradient_checkpointing_enable()` instead, or if you are using the `Trainer` API, pass `gradient_checkpointing=True` in your `TrainingArguments`.
  "Passing `gradient_checkpointing` to a config initialization is deprecated and will be removed in v5 "
What is the name of the bridge?
Setting `pad_token_id` to `eos_token_id`:50256 for open-end generation.
What is the name of the bridge?

The bridge is named after the Greek goddess of the sea, the goddess of the sea.

The bridge is named after the Greek goddess of the sea, the goddess of the sea. The bridge is named after the Greek goddess of the sea, the goddess of the sea. The bridge is named after the Greek goddess of the sea, the goddess of the sea. The bridge is named after the Greek goddess of the sea, the goddess of the sea. The bridge is named after the Greek goddess of the sea, the goddess of the sea. The bridge is named after the Greek goddess of the sea, the goddess of the sea. The bridge is named after the Greek goddess of the sea, the goddess of the sea. The bridge is named after the Greek goddess of the sea, the goddess of the sea. The bridge is named after the Greek goddess of the sea, the goddess of the sea. The bridge is named after the Greek goddess
What is the IP address of the bridge?
Setting `pad_token_id` to `eos_token_id`:50256 for open-end generation.
What is the IP address of the bridge?

The IP address of the bridge is the address of the bridge.

What is the name of the bridge?

The name of the bridge is the name of the bridge.

What is the name of the bridge's address?

The name of the bridge's address is the address of the bridge.

What is the name of the bridge's address's address?

The name of the bridge's address's address is the address of the bridge.

What is the name of the bridge's address's address's address?

The name of the bridge's address's address's address's address's address's address's address's address's address's address's address's address's address's address's address's address's address's address's address's address's address's address's address's address's address's address's address's address's address's address's address's address's address's address's
What is the subnet mask of the bridge?
Setting `pad_token_id` to `eos_token_id`:50256 for open-end generation.
What is the subnet mask of the bridge?

The subnet mask is the number of bits of the bridge that are connected to the bridge.

The subnet mask is the number of bits of the bridge that are connected to the bridge. The bridge is connected to the subnet mask.

The bridge is connected to the subnet mask. The bridge is connected to the subnet mask.

The bridge is connected to the subnet mask. The bridge is connected to the subnet mask.

The bridge is connected to the subnet mask. The bridge is connected to the subnet mask.

The bridge is connected to the subnet mask. The bridge is connected to the subnet mask.

The bridge is connected to the subnet mask. The bridge is connected to the subnet mask.

The bridge is connected to the subnet mask. The bridge is connected to the subnet mask.

The bridge is connected
What is the default gateway of the bridge?
Setting `pad_token_id` to `eos_token_id`:50256 for open-end generation.
What is the default gateway of the bridge?

The default gateway of the bridge is the default gateway of the bridge.

What is the default gateway of the bridge?

The default gateway of the bridge is the default gateway of the bridge.

What is the default gateway of the bridge?

The default gateway of the bridge is the default gateway of the bridge.

What is the default gateway of the bridge?

The default gateway of the bridge is the default gateway of the bridge.

What is the default gateway of the bridge?

The default gateway of the bridge is the default gateway of the bridge.

What is the default gateway of the bridge?

The default gateway of the bridge is the default gateway of the bridge.

What is the default gateway of the bridge?

The default gateway of the bridge is the default gateway of the bridge.

What is the default gateway of the bridge?
